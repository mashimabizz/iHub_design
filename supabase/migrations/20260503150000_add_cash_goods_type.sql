-- =====================================================================
-- iter65.7: 「定価」種別を goods_types_master に追加
-- =====================================================================
-- ユーザー要望: wish / 個別募集で求めるものを選ぶとき「定価で買う」選択肢を出したい。
-- 同じ goods_type select の中に option として表示するのが最もシンプル。

insert into public.goods_types_master (name, category, display_order)
values ('定価', 'other', 90)
on conflict (name) do nothing;

-- =====================================================================
-- 完了
-- =====================================================================
